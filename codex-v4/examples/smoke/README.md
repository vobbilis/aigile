# Double Smoke Fixture

This empty Python project tests Codex plan/build execution without changing the
dashboard. Python and pytest are supplied by the caller's virtual environment.

Implement `double(value: int) -> int` in `double.py`. Return twice the input.
Accept positive integers, zero, and negative integers. Add `test_double.py` with
one test named `test_double` covering all three cases. No dependencies, network
access, subprocesses, configuration changes, or other files are needed.